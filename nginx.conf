server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript 
               application/x-javascript application/xml+rss 
               application/json application/javascript;

    # Обработка /my-resume/assets/ -> /assets/
    location /my-resume/assets/ {
        alias /usr/share/nginx/html/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Обработка /my-resume/icons/ -> /icons/
    location /my-resume/icons/ {
        alias /usr/share/nginx/html/icons/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Обработка /my-resume/vite.svg -> /vite.svg
    location /my-resume/vite.svg {
        alias /usr/share/nginx/html/vite.svg;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Основной путь /my-resume/
    location /my-resume/ {
        try_files /index.html =404;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }

    # Редирект с корня на /my-resume/
    location = / {
        return 301 /my-resume/;
    }

    # Фавикон и robots
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }

    location = /robots.txt {
        log_not_found off;
        access_log off;
    }

    error_page 404 /index.html;
}
